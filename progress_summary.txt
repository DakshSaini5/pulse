PROGRESS SUMMARY (2026-06-12)

1. Mobile vs Web UI Separation:
   - Modified `Layout.tsx` to conditionally render the "v0 design" mobile shell ONLY when running natively on mobile devices (Android/iOS) via Capacitor.
   - For regular web browsers, the app continues to serve the beautiful standard desktop web layout.

2. v0 Design Integration (Mobile):
   - Refactored `DiscoverScreen.tsx`, `RecordsScreen.tsx`, `TrendsScreen.tsx`, and `HospitalCompareScreen.tsx` to integrate perfectly inside the fixed 390px mobile layout.
   - Removed duplicate navigation wrappers and used the `MobileBottomNav` inside the Layout shell.
   - Fixed the "PANIC ATTACK" emergency modal logic to match the v0 design surgical changes.
   - Added `oklch` theme color variables to `index.css`.

3. Google Sign-In Fixes:
   - Fixed the "Network Error" on mobile by adding `capacitor://localhost` and `http://localhost` to the backend CORS allowed origins (`index.ts`).
   - Updated `api.ts` to dynamically use `import.meta.env.VITE_API_URL` for the backend URL, falling back to local IPs.

PENDING TASKS FOR NEXT SESSION:
1. "Hospital Near Me" feature rebuild.
2. Fix any remaining "pathetic" CSS/Tailwind conflicts on the web app (ensure temp_vars.css isn't overriding anything).
3. Finalizing web deployment (domain mapping) and Play Store submission.
4. Testing Google Sign-in on a physical mobile device with the new CORS rules.
